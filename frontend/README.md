# VertexTrade — frontend

The public marketing site. See the [root README](../README.md) for the full
project overview, architecture, and setup instructions.

Quick start: `npm install && npm run dev` (runs on `:5173`).
