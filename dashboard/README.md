# VertexTrade — dashboard

The trading dashboard (watchlist, orders, holdings, positions, summary). See
the [root README](../README.md) for the full project overview, architecture,
and setup instructions.

Quick start: `npm install && npm run dev` (runs on `:5174`). Requires the
backend running on `:3002` (see `../backend`).
